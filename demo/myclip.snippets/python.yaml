## myclip snippets
  - caption: python import from sibling dir
    desc: | 
      http://stackoverflow.com/questions/4542352/import-from-sibling-directory
    body: | 
      import inspect
      currentdir = os.path.dirname(os.path.abspath(inspect.getfile(inspect.currentframe())))
      parentdir = os.path.dirname(currentdir)    
      os.sys.path.insert(0,parentdir+'/libpy')   

  - caption: python unicode docnew
    desc: | 
      blank file template for unicode python file
      python 2.x
    body: | 
      #!/usr/bin/python
      # -*- coding: utf-8 -*-

  - caption: python try catch
    domain:  python
    tags:    try, catch, exception
    format:  plaintext
    body: | 
      ## get full details for python exception
      ## including the filename and line number
      try:
        MainProcedure()
      except Exception as msg:
        print 'UNEXPECTED TERMINATION msg@%s'%(msg.__repr__())
        exc_type, exc_obj, exc_tb = sys.exc_info()
        fname = os.path.split(exc_tb.tb_frame.f_code.co_filename)[1]
        print(exc_type, fname, exc_tb.tb_lineno)
